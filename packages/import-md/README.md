# Import to Markdown

Convert Word, Excel, BibTeX, and selectable-text PDF files into AI-ready Markdown.

Drop or select a file and the app produces a clean `.md` conversion in your workspace. DOCX images are extracted into a sibling assets folder. Complex Office features and scanned PDFs are detected and warned about rather than silently mangled.
