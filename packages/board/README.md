# Board

Kanban issue board for tracking work in your workspace.

Create, prioritise, and manage issues through the board UI or the AI chat. Issues live in the workspace `issues/` folder as plain files, so they travel with the project and are visible to every tool.

**Tools:** `issues.list`, `issues.get`, `issues.create`, `issues.update`, `issues.delete`
