# DOCX Review

Structured academic peer-review workflow for Word documents.

Upload a `.docx` manuscript and the app runs a multi-agent review: each reviewer evaluates the document from a different angle, then a reconciliation step produces a final assessment. The output is a reviewed `.docx` with inline comments written back into the file, plus a markdown summary report.
