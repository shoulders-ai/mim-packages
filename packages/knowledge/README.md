# Knowledge

Workspace knowledge base for capturing and retrieving persistent notes, decisions, and context.

Add entries through the UI or chat. The AI can query the knowledge base to ground its answers in workspace-specific facts. Entries are stored in the `knowledge/` folder as plain files.

**Tools:** `knowledge.list`, `knowledge.get`, `knowledge.create`, `knowledge.update`, `knowledge.delete`
